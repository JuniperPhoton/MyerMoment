<Page
    x:Class="MyerMomentUniversal.StorePage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:MyerMomentUniversal"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d" Background="{StaticResource MomentThemeColor}">
    <Page.Transitions>
        <TransitionCollection>
            <NavigationThemeTransition>
                <NavigationThemeTransition.DefaultNavigationTransitionInfo>
                    <SlideNavigationTransitionInfo/>
                </NavigationThemeTransition.DefaultNavigationTransitionInfo>
            </NavigationThemeTransition>
        </TransitionCollection>
    </Page.Transitions>
    <Grid Background="{StaticResource MomentThemeBlack}">
        <Grid.RowDefinitions>
            <RowDefinition Height="70"/>
            <RowDefinition/>
        </Grid.RowDefinitions>
        <Grid Background="{StaticResource MomentThemeColor}">
            <TextBlock x:Uid="StoreTB" Text="Store" Foreground="{StaticResource MomentThemeBlack}" FontFamily="Segoe WP" FontSize="40" VerticalAlignment="Top" HorizontalAlignment="Left" Margin="10,0,0,0" FontWeight="Light"/>
        </Grid>
        <local:StoreContentControl Grid.Row="1"/>
    </Grid>
</Page>
